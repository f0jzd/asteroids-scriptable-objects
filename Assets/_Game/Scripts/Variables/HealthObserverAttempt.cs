using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Variables;

public class HealthObserverAttempt : MonoBehaviour
{

    [SerializeField] private IntObservable _intObservable;
    
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    
}
